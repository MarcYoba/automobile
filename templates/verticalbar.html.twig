<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

    <!-- Sidebar - Brand -->
    {% if app.user.employer is not null %}
        <a class="sidebar-brand d-flex align-items-center justify-content-center" href="{{ path('app_dashboard', {'id': app.user.employer.agence.id}) }}">
            <div class="sidebar-brand-icon rotate-n-15">
                <i class="fas fa-laugh-wink"></i>
            </div>
            <div class="sidebar-brand-text mx-3">AB COMPTA <sup>2</sup></div>
        </a>
    {% else %}
        {% if agences is defined and agences|length > 0 %}
            {% for temp in app.user.tempAgences %}
                <a class="sidebar-brand d-flex align-items-center justify-content-center" href="{{ path('app_dashboard', {'id': temp.agence.id}) }}">
                    <div class="sidebar-brand-icon rotate-n-15">
                        <i class="fas fa-laugh-wink"></i>
                    </div>
                    <div class="sidebar-brand-text mx-3">AB COMPTA <sup>2</sup></div>
                </a>
            {% endfor %}
        {% else %}
            <a class="sidebar-brand d-flex align-items-center justify-content-center" href="{{ path('app_agence') }}">
                <div class="sidebar-brand-icon rotate-n-15">
                    <i class="fas fa-laugh-wink"></i>
                </div>
                <div class="sidebar-brand-text mx-3">AB COMPTA <sup>2</sup></div>
            </a>
        {% endif %}
    {% endif %}

    <!-- Divider -->
    <hr class="sidebar-divider my-0">

    <!-- Nav Item - Dashboard -->
    <li class="nav-item active">
        {% if app.user.employer is not null %}
            <a class="nav-link" href="{{ path('app_dashboard', {'id': app.user.employer.agence.id}) }}">
                <i class="fas fa-fw fa-tachometer-alt"></i>
                <span>Dashboard</span>
            </a>
        {% else %}
            <a class="nav-link" href="{{ path('app_agence') }}">
                <i class="fas fa-fw fa-home"></i>
                <span>Home</span>
            </a>
        {% endif %}
    </li>

    <!-- Vente Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapsevente"
            aria-expanded="true" aria-controls="collapsevente">
            <i class="fas fa-fw fa-shopping-cart"></i>
            <span>Vente</span>
        </a>
        <div id="collapsevente" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Opérations de vente</h6>
                <a class="collapse-item" href="{{ path('app_vente') }}">Ajouter une vente</a>
                <a class="collapse-item" href="{{ path('vente_list') }}">Liste des ventes</a>
            </div>
        </div>
    </li>

    <!-- Achat Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseachat"
            aria-expanded="true" aria-controls="collapseachat">
            <i class="fas fa-fw fa-shopping-basket"></i>
            <span>Achat</span>
        </a>
        <div id="collapseachat" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Opérations d'achat</h6>
                <a class="collapse-item" href="{{ path('app_achat') }}">Ajouter un achat</a>
                <a class="collapse-item" href="{{ path('achat_list') }}">Liste des achats</a>
            </div>
        </div>
    </li>

    <!-- Client Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseclient"
            aria-expanded="true" aria-controls="collapseclient">
            <i class="fas fa-fw fa-users"></i>
            <span>Client</span>
        </a>
        <div id="collapseclient" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Gestion des clients</h6>
                {% if "ROLE_ADMIN_ADMIN" not in app.user.roles %}
                    <a class="collapse-item" href="{{ path('app_register') }}">Ajouter un client</a>
                {% endif %}
                <a class="collapse-item" href="#">Liste des clients</a>
                {% if "ROLE_ADMIN_ADMIN" not in app.user.roles %}
                    <a class="collapse-item" href="#">Clients inactifs</a>
                {% endif %}
            </div>
        </div>
    </li>

    <!-- Produit Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseproduit"
            aria-expanded="true" aria-controls="collapseproduit">
            <i class="fas fa-fw fa-box"></i>
            <span>Produit</span>
        </a>
        <div id="collapseproduit" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Gestion des produits</h6>
                <a class="collapse-item" href="{{ path('app_produit') }}">Ajouter un produit</a>
                <a class="collapse-item" href="{{ path('produit_list') }}">Liste des produits</a>
            </div>
        </div>
    </li>

    <!-- Fournisseur Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapsefournisseur"
            aria-expanded="true" aria-controls="collapsefournisseur">
            <i class="fas fa-fw fa-truck"></i>
            <span>Fournisseur</span>
        </a>
        <div id="collapsefournisseur" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Gestion des fournisseurs</h6>
                <a class="collapse-item" href="{{ path('app_fournisseur') }}">Ajouter un fournisseur</a>
                <a class="collapse-item" href="{{ path('fournisseur_list') }}">Liste des fournisseurs</a>
            </div>
        </div>
    </li>

    <!-- Agence Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseAgence"
            aria-expanded="true" aria-controls="collapseAgence">
            <i class="fas fa-fw fa-building"></i>
            <span>Agence</span>
        </a>
        <div id="collapseAgence" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Gestion des agences</h6>
                {% if "ROLE_ADMIN_ADMIN" in app.user.roles %}
                    <a class="collapse-item" href="{{ path('app_agence_new') }}">Ajouter une agence</a>
                {% endif %}
                <a class="collapse-item" href="{{ path('app_agence_list') }}">Liste des agences</a>
            </div>
        </div>
    </li>

    <!-- Stock Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapsestock"
            aria-expanded="true" aria-controls="collapsestock">
            <i class="fas fa-fw fa-warehouse"></i>
            <span>Stock</span>
        </a>
        <div id="collapsestock" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Gestion des stocks</h6>
                <a class="collapse-item" href="{{ path('app_historique') }}">Historique</a>
                <a class="collapse-item" href="{{ path('app_quantite_stock') }}">Quantité stock</a>
                <a class="collapse-item" href="#">Récapitulatif</a>
                <a class="collapse-item" href="#">Stock initial / Inventaire</a>
            </div>
        </div>
    </li>

    <!-- Versement Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseversement"
            aria-expanded="true" aria-controls="collapseversement">
            <i class="fas fa-fw fa-money-bill-wave"></i>
            <span>Versement</span>
        </a>
        <div id="collapseversement" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Gestion des versements</h6>
                <a class="collapse-item" href="{{ path('app_versement') }}">Ajouter un versement</a>
                <a class="collapse-item" href="{{ path('versement_list') }}">Liste des versements</a>
            </div>
        </div>
    </li>

    <!-- Caisse Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapsecaisse"
            aria-expanded="true" aria-controls="collapsecaisse">
            <i class="fas fa-fw fa-cash-register"></i>
            <span>Caisse</span>
        </a>
        <div id="collapsecaisse" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Gestion de caisse</h6>
                <a class="collapse-item" href="{{ path('app_caisse') }}">Caisse</a>
                <a class="collapse-item" href="{{ path('app_caisse_list') }}">Liste des caisses</a>
            </div>
        </div>
    </li>

    <!-- Utilisateur Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseutilisateur"
            aria-expanded="true" aria-controls="collapseutilisateur">
            <i class="fas fa-fw fa-user"></i>
            <span>Utilisateur</span>
        </a>
        <div id="collapseutilisateur" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Gestion des utilisateurs</h6>
                {% if "ROLE_ADMIN_ADMIN" in app.user.roles %}
                    <a class="collapse-item" href="{{ path('user_list') }}">Liste des utilisateurs</a>
                {% endif %}
                <a class="collapse-item" href="{{ path('app_register') }}">Ajouter un utilisateur</a>
            </div>
        </div>
    </li>

    <!-- Dépense Section -->
    <li class="nav-item">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapsedepense"
            aria-expanded="true" aria-controls="collapsedepense">
            <i class="fas fa-fw fa-credit-card"></i>
            <span>Dépense</span>
        </a>
        <div id="collapsedepense" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Gestion des dépenses</h6>
                <a class="collapse-item" href="{{ path('app_depenses') }}">Ajouter une dépense</a>
                {% if app.user.employer is not null %}
                    <a class="collapse-item" href="{{ path('depenses_list', {'id': app.user.employer.agence.id}) }}">Liste des dépenses</a>
                {% else %}
                    <a class="collapse-item" href="#">Liste des dépenses</a>
                {% endif %}
            </div>
        </div>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider">

    <!-- Heading -->
    <div class="sidebar-heading">
        Interface
    </div>

    <!-- Rest of the sections (Employé, Journal Comptable, Analyse Évolutive, etc.) -->
    <!-- ... le reste du code reste similaire avec les mêmes corrections ... -->

    <!-- Sidebar Toggler (Sidebar) -->
    <div class="text-center d-none d-md-inline">
        <button class="rounded-circle border-0" id="sidebarToggle"></button>
    </div>

</ul>